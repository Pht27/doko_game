name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build frontend
        run: |
          cd src/frontend
          npm ci
          npm run build

      - name: Upload frontend to production
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "src/frontend/dist/"
          target: "/root/opt/doko-frontend/"
          strip_components: 3

      - name: Rebuild production backend
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            cd ${{ secrets.DEPLOY_PATH }}
            git pull origin main
            docker compose -f src/infrastructure/docker-compose.staging.yml \
              --env-file src/infrastructure/.env \
              up --build -d --no-deps backend
            docker exec caddy caddy reload --config /etc/caddy/Caddyfile
